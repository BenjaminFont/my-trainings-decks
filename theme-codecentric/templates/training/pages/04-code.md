---
layout: cc
---

<span class="secno">04</span>
<h2 class="slash">Code zeigen</h2>

```ts
// Code-Blöcke werden im Marken-Dunkelstil gerendert (abgerundet, Schatten).
export function greet(name: string): string {
  return `Hallo, ${name}!`
}
```

<p class="note">Inline-Code: <code class="inline">npm run dev</code> startet den Dev-Server auf <code class="inline">localhost:3030</code>.</p>

<!--
Code-Folie. Fenced Code-Blocks bekommen automatisch den dunklen Karten-Stil.
Inline-Code mit <code class="inline">…</code>. .note für dezente Hinweise.
-->
